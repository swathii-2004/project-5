# ─────────────────────────────────────────────
# ProxiMart — Frontend (Admin app)
# ─────────────────────────────────────────────

# Base URL for all REST API calls
# No WebSocket needed for admin — chat is user/vendor only (Phase 6)
VITE_API_URL=http://localhost:8000/api/v1
